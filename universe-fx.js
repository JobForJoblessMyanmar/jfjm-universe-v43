(() => {
  if (matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  const canvas=document.createElement('canvas'), horizon=document.createElement('div');canvas.className='universe-fx-canvas';horizon.className='universe-fx-horizon';document.body.prepend(horizon);document.body.prepend(canvas);
  const ctx=canvas.getContext('2d'), pointer={x:.5,y:.36};let w=0,h=0,stars=[],dust=[],raf=0;
  function reset(){const r=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*r;canvas.height=h*r;ctx.setTransform(r,0,0,r,0,0);const dense=innerWidth<720?35:88;stars=Array.from({length:dense},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.45+.25,s:Math.random()*.18+.025,p:Math.random()*7,c:Math.random()>.6?'#ffcef1':'#bffaff'}));dust=Array.from({length:innerWidth<720?2:5},()=>({x:Math.random()*w,y:Math.random()*h,r:70+Math.random()*110,h:Math.random()*360}))}
  function draw(t){ctx.clearRect(0,0,w,h);const x=w*(.25+pointer.x*.5),y=h*(.15+pointer.y*.32),g=ctx.createRadialGradient(x,y,0,x,y,Math.max(w,h)*.48);g.addColorStop(0,'rgba(72,234,255,.13)');g.addColorStop(.43,'rgba(150,77,255,.065)');g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);dust.forEach((d,i)=>{d.x+=Math.sin(t*.00017+i)*.08;d.y-=.05; if(d.y<-d.r)d.y=h+d.r;const q=ctx.createRadialGradient(d.x,d.y,0,d.x,d.y,d.r);q.addColorStop(0,`hsla(${d.h},90%,70%,.045)`);q.addColorStop(1,'transparent');ctx.fillStyle=q;ctx.fillRect(d.x-d.r,d.y-d.r,d.r*2,d.r*2)});stars.forEach((s,i)=>{s.y-=s.s;if(s.y<-5){s.y=h+5;s.x=Math.random()*w}const a=.32+Math.sin(t*.0015+s.p+i)*.3;ctx.beginPath();ctx.arc(s.x,s.y,s.r+a,0,7);ctx.fillStyle=s.c;ctx.globalAlpha=Math.max(.08,a+.32);ctx.fill();ctx.globalAlpha=1});raf=requestAnimationFrame(draw)}
  addEventListener('pointermove',e=>{pointer.x=e.clientX/w;pointer.y=e.clientY/h},{passive:true});addEventListener('resize',reset,{passive:true});reset();draw(0);addEventListener('pagehide',()=>cancelAnimationFrame(raf),{once:true});
})();
